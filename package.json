{
  "name": "calorie-counter",
  "version": "1.0.0",
  "description": "Calorie counter web app",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js --watch public/**/*.html --watch public/**/*.css --watch public/**/*.js --watch server.js"
  },
  "dependencies": {
    "express": "^4.21.2"
  },
  "devDependencies": {
    "nodemon": "^3.1.9"
  }
}
